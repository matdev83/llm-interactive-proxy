# Suppress unreachable code warnings for empty async generators
# These are intentionally structured as async generators that return immediately
# Format expected by test: just the names, one per line
return
yield
allow_tool_retry
refresh_token_callback
# Backward-compatible parameter maintained for test patching
reasoning_output_length
# Used in type annotations (string literals) - vulture can't detect these
IKiloToolTranslator
ISessionDetector
# Used in conditional import and type comments
WebSocketClientProtocol
# __exit__ and __aexit__ parameters are required by protocol
exc_type
exc_val
exc_tb
# Re-exported from backend_imports.__all__ (imported only for public API surface)
reset_backend_discovery_state
# Protocol / stub signatures (Pydantic model_dump keyword)
exclude_none