qwen -y -p "Headless Mode: You are being operated in a **headless, non-interactive mode**. Do not ask any questions or look for confirmations, nobody is monitoring the console, just execute the task according to the following prompt: Git commit workflow \n 1. Use `git status` command to see all local changes. \n 2. Create a commit message for those changes. \n 3. If changes are substantial and intrudoce new user-facing functionalities, check if you need to also create a new entry at the top of the CHANGELOG.md file. But only touch CHANGELOG.md if there's a very good reason to do so, avoid putting there information about small and not important changes. \n 4. Stage all changes and commit to local dev. \n 5. Do not run tests, do not touch any other files"