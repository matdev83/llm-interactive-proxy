$schema: "http://json-schema.org/draft-07/schema#"
title: App Config
type: object
additionalProperties: false
properties:
  host: { type: string }
  port: { type: integer, minimum: 1 }
  public_url: { type: ["string", "null"] }
  anthropic_port: { type: ["integer", "null"], minimum: 1 }
  proxy_timeout: { type: integer, minimum: 0 }
  command_prefix: { type: string }
  strict_command_detection: { type: boolean }
  context_window_override: { type: ["integer", "null"], minimum: 1 }
  gcp_project_id: { type: ["string", "null"] }
  gemini_credentials_path: { type: ["string", "null"] }
  disable_health_checks: { type: boolean }
  enable_activity_tracking: { type: boolean }
  request_dedup_window: { type: number, minimum: 0 }
  request_dedup_max_cache: { type: integer, minimum: 1 }
  default_rate_limit: { type: integer, minimum: 0 }
  default_rate_window: { type: integer, minimum: 0 }
  model_defaults: { type: object, additionalProperties: true }
  failover_routes: { type: object, additionalProperties: true }
  identity:
    type: object
    additionalProperties: false
    properties:
      session_id: { type: ["string", "null"] }
      session_turn_count: { type: integer, minimum: 0 }
      title:
        type: object
        additionalProperties: false
        properties:
          mode: { type: string, enum: [default, passthrough, override] }
          override_value: { type: ["string", "null"] }
          default_value: { type: string }
          passthrough_name: { type: string }
      url:
        type: object
        additionalProperties: false
        properties:
          mode: { type: string, enum: [default, passthrough, override] }
          override_value: { type: ["string", "null"] }
          default_value: { type: string }
          passthrough_name: { type: string }
      user_agent:
        type: object
        additionalProperties: false
        properties:
          mode: { type: string, enum: [default, passthrough, override] }
          override_value: { type: ["string", "null"] }
          default_value: { type: string }
          passthrough_name: { type: string }
  empty_response:
    type: object
    additionalProperties: false
    properties:
      enabled:
        type: boolean
        description: "Whether empty response recovery is enabled"
      max_retries:
        type: integer
        minimum: 0
        description: "Maximum number of retries for empty responses"
  edit_precision:
    type: object
    additionalProperties: false
    properties:
      enabled:
        type: boolean
        description: "Master switch for edit-precision tuning"
      temperature:
        type: number
        minimum: 0.0
        maximum: 2.0
        description: "Temperature override for edit-precision mode"
      min_top_p:
        type: ["number", "null"]
        minimum: 0.0
        maximum: 1.0
        description: "Minimum top_p value when override_top_p is true"
      override_top_p:
        type: boolean
        description: "Whether to override top_p in edit-precision mode"
      override_top_k:
        type: boolean
        description: "Whether to override top_k in edit-precision mode"
      target_top_k:
        type: ["integer", "null"]
        minimum: 1
        description: "Target top_k value when override_top_k is true"
      exclude_agents_regex:
        type: ["string", "null"]
        description: "Regex pattern to exclude agents from edit-precision mode"
  rewriting:
    type: object
    additionalProperties: false
    properties:
      enabled:
        type: boolean
        description: "Whether content rewriting is enabled"
      config_path:
        type: string
        description: "Path to directory containing rewriting configuration files"
  app: { type: ["object", "null"] }
  logging:
    type: object
    additionalProperties: false
    properties:
      level: { type: string, enum: [TRACE, DEBUG, INFO, WARNING, ERROR, CRITICAL] }
      request_logging: { type: boolean }
      response_logging: { type: boolean }
      console_stream: { type: string, enum: [stdout, stderr] }
      log_file: { type: ["string", "null"] }
      capture_file: { type: ["string", "null"] }
      capture_max_bytes: { type: ["integer", "null"], minimum: 0 }
      capture_truncate_bytes: { type: ["integer", "null"], minimum: 0 }
      capture_max_files: { type: ["integer", "null"], minimum: 0 }
      capture_rotate_interval_seconds: { type: ["integer", "null"], minimum: 0 }
      capture_total_max_bytes: { type: ["integer", "null"], minimum: 0 }
      capture_buffer_size: { type: ["integer", "null"], minimum: 1024 }
      capture_flush_interval: { type: ["number", "null"], minimum: 0.1 }
      capture_max_entries_per_flush: { type: ["integer", "null"], minimum: 1 }
      cbor_capture_dir: { type: ["string", "null"] }
      cbor_capture_session_id: { type: ["string", "null"] }
      cbor_capture_flush_interval: { type: ["number", "null"], minimum: 0.1 }
      use_colors: { type: boolean }
  auth:
    type: object
    additionalProperties: false
    properties:
      disable_auth: { type: boolean }
      api_keys:
        type: array
        items: { type: string }
      auth_token: { type: ["string", "null"] }
      redact_api_keys_in_prompts: { type: boolean }
      trusted_ips:
        type: array
        items: { type: string }
      brute_force_protection:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean, default: true }
          max_failed_attempts: { type: integer, minimum: 1 }
          ttl_seconds: { type: integer, minimum: 1 }
          initial_block_seconds: { type: integer, minimum: 1 }
          block_multiplier: { type: number, minimum: 1 }
          max_block_seconds: { type: integer, minimum: 1 }
  sso:
    type: ["object", "null"]
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      session_lifetime_hours: { type: integer, minimum: 1 }
      database_path: { type: string }
      providers:
        type: object
        additionalProperties:
          type: object
          additionalProperties: false
          properties:
            type: { type: string, enum: [oauth2, saml] }
            client_id: { type: string }
            client_secret: { type: string }
            enabled: { type: boolean }
            discovery_url: { type: ["string", "null"] }
            metadata_url: { type: ["string", "null"] }
            authorize_url: { type: ["string", "null"] }
            token_url: { type: ["string", "null"] }
            userinfo_url: { type: ["string", "null"] }
            scopes:
              type: array
              items: { type: string }
      authorization:
        type: object
        additionalProperties: false
        properties:
          mode: { type: string, enum: [single_user, enterprise] }
          api_url: { type: ["string", "null"] }
          api_timeout: { type: integer, minimum: 1 }
          confirmation_code_expiry_minutes: { type: integer, minimum: 1 }
          max_confirmation_attempts: { type: integer, minimum: 1 }
      captcha:
        type: ["object", "null"]
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          provider: { type: string, enum: [cloudflare_turnstile] }
          site_key: { type: ["string", "null"] }
          secret_key: { type: ["string", "null"] }
          verify_url: { type: string }
          widget_mode: { type: string, enum: [invisible, managed] }
          timeout_seconds: { type: number, minimum: 0 }
  session:
    type: object
    additionalProperties: false
    properties:
      cleanup_enabled: { type: boolean }
      cleanup_interval: { type: integer, minimum: 0 }
      max_age: { type: integer, minimum: 0 }
      default_interactive_mode: { type: boolean }
      force_set_project: { type: boolean }
      project_dir_resolution_model: { type: ["string", "null"] }
      project_dir_resolution_mode: { type: string, enum: ["disabled", "deterministic", "llm", "hybrid"] }
      project_dir_resolution_filesystem_mode: { type: string, enum: ["auto", "enabled", "disabled"] }
      disable_default_openrouter_project_dir_resolution_fallback: { type: boolean }
      disable_interactive_commands: { type: boolean }
      tool_call_repair_enabled: { type: boolean }
      json_repair_enabled: { type: boolean }
      json_repair_strict_mode: { type: boolean }
      json_repair_schema: { type: ["object", "null"] }
      json_repair_buffer_cap_bytes: { type: integer, minimum: 0 }
      tool_call_repair_buffer_cap_bytes: { type: integer, minimum: 0 }
      streaming_sampler:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          sample_rate: { type: number, minimum: 0, maximum: 1 }
          max_samples: { type: integer, minimum: 1 }
      dangerous_command_prevention_enabled: { type: boolean }
      dangerous_command_steering_message: { type: ["string", "null"] }
      pytest_full_suite_steering_enabled: { type: boolean }
      pytest_full_suite_steering_message: { type: ["string", "null"] }
      test_execution_reminder_enabled: { type: boolean }
      test_execution_reminder_message: { type: ["string", "null"] }
      fix_think_tags_enabled: { type: boolean }
      fix_think_tags_streaming_buffer_size: { type: integer, minimum: 1024 }
      droid_path_fix_enabled: { type: boolean }
      double_ampersand_fixes_for_windows_enabled: { type: boolean }
      auto_continue_removal_enabled: { type: boolean }
      streaming_loop_detection_enabled: { type: boolean }
      max_per_session_backends: { type: integer, minimum: 1 }
      session_continuity:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          fingerprint_message_count: { type: integer, minimum: 1 }
          fuzzy_matching: { type: boolean }
          fuzzy_window_sizes: { type: array, items: { type: integer, minimum: 1 } }
          max_session_age_seconds: { type: integer, minimum: 0 }
          client_key_includes_ip: { type: boolean }
          enable_topic_similarity_matching: { type: boolean }
      b2bua:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          continuity_max_age_seconds: { type: integer, minimum: 1 }
          continuity_sliding_expiration: { type: boolean }
          persistent_mapping_store_enabled: { type: boolean }
          echo_enabled: { type: boolean }
          echo_header_name: { type: string, minLength: 1 }
          enable_unsafe_heuristic_session_inference: { type: boolean }
          deployment_mode: { type: string, enum: ["single-process", "multi-worker"] }
      client_compatibility:
        type: object
        additionalProperties: false
        properties:
          reasoning_mode_header: { type: string }
          reasoning_meaningful_header: { type: string }
          user_agent_rules:
            type: array
            items:
              type: object
              additionalProperties: false
              properties:
                name: { type: string, minLength: 1 }
                enabled: { type: boolean }
                user_agent_regex: { type: string, minLength: 1 }
                reasoning_mode:
                  type: string
                  enum: ["passthrough", "coerce_to_content", "drop"]
                reasoning_counts_as_meaningful: { type: boolean }
      planning_phase:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          strong_model: { type: ["string", "null"] }
          max_turns: { type: integer, minimum: 1 }
          max_file_writes: { type: integer, minimum: 0 }
          overrides:
            type: ["object", "null"]
            additionalProperties: false
            properties:
              temperature: { type: ["number", "null"] }
              top_p: { type: ["number", "null"] }
              reasoning_effort: { type: ["string", "null"] }
              thinking_budget: { type: ["integer", "null"], minimum: 0 }
              reasoning: { type: ["object", "null"] }
              generation_config: { type: ["object", "null"] }
      tool_call_reactor:
        type: object
        additionalProperties: true
        properties:
          enabled: { type: boolean }
          unified_steering_enabled: { type: boolean }
          steering_policy_priorities: { type: ["object", "null"] }
          steering_session_ttl_seconds: { type: integer, minimum: 0 }
          steering_max_sessions: { type: integer, minimum: 1 }
          apply_diff_steering_enabled: { type: boolean }
          apply_diff_steering_rate_limit_seconds: { type: integer, minimum: 0 }
          apply_diff_steering_message: { type: ["string", "null"] }
          pytest_full_suite_steering_enabled: { type: boolean }
          pytest_full_suite_steering_message: { type: ["string", "null"] }
          inline_python_steering_enabled: { type: boolean }
          inline_python_steering_message: { type: ["string", "null"] }
          binary_file_edit_steering_enabled: { type: boolean }
          binary_file_edit_steering_message: { type: ["string", "null"] }
          pytest_context_saving_enabled: { type: boolean }
          fix_think_tags_enabled: { type: boolean }
          test_execution_reminder_enabled: { type: boolean }
          test_execution_reminder_message: { type: ["string", "null"] }
          access_policies:
            type: array
            items: { type: object }
          steering_rules:
            type: array
            items: { type: object }
      tool_access_global_overrides: { type: ["object", "null"] }
      force_reprocess_tool_calls: { type: boolean }
      log_skipped_tool_calls: { type: boolean }
      quality_verifier_model:
        type: ["string", "null"]
        description: >
          Model selector for the quality verifier. Supports composite routing (`|` failover, `^` weighted)
          and URI-style parameters (e.g. `model?temperature=0.5`). Mixing `|` and `^` in one selector is rejected.
      quality_verifier_frequency:
        type: integer
        minimum: 1
        default: 1
      quality_verifier_tool_followup_weight:
        type: number
        minimum: 0.0
        maximum: 1.0
        default: 0.2
      quality_verifier_max_history: { type: ["integer", "null"], minimum: 1 }
      quality_verifier_max_consecutive_failures: { type: integer, minimum: 1 }
      quality_verifier_cooldown_seconds: { type: integer, minimum: 0 }
      quality_verifier_ttft_timeout_seconds: { type: number }
  backends:
    type: object
    additionalProperties: true
    properties:
      default_backend: { type: string }
      static_route:
        type: ["string", "null"]
        description: >
          Optional forced routing selector applied to requests (strict `backend:model`).
          Composite routing syntax is supported (`|`, `^`, `[weight=N]`), and mixing operators is rejected.
      disable_gemini_oauth_fallback: { type: boolean }
      disable_hybrid_backend: { type: boolean }
      hybrid_backend_repeat_messages: { type: boolean }
      reasoning_injection_probability: { type: number, minimum: 0.0, maximum: 1.0 }
      hybrid_reasoning_model_timeout: { type: integer, minimum: 1 }
      hybrid_reasoning_force_initial_turns: { type: integer, minimum: 0 }
      hybrid_execution_model_timeout: { type: integer, minimum: 1 }
      hybrid_reasoning_latency_threshold: { type: number, minimum: 0.0 }
      hybrid_reasoning_backoff_turns: { type: integer, minimum: 0 }
      openai_codex:
        type: object
        additionalProperties: true
        properties:
          timeout: { type: integer, minimum: 1 }
          extra:
            type: object
            additionalProperties: true
            properties:
              codex:
                type: object
                additionalProperties: true
                properties:
                  managed_oauth:
                    type: object
                    additionalProperties: false
                    properties:
                      enabled: { type: boolean }
                      storage_path: { type: string, minLength: 1 }
                      accounts:
                        oneOf:
                          - type: string
                            enum: ["all"]
                          - type: array
                            items: { type: string, minLength: 1 }
                      selection_strategy:
                        type: string
                        enum:
                          - round-robin
                          - random
                          - first-available
                          - session-affinity
                      refresh_buffer_seconds: { type: integer, minimum: 0 }
                      session_affinity_ttl_seconds: { type: integer, minimum: 1 }
                      session_affinity_max_entries: { type: integer, minimum: 1 }
                      allow_legacy_fallback: { type: boolean }
  reasoning_aliases:
    type: object
    additionalProperties: false
    properties:
      reasoning_alias_settings:
        type: array
        items:
          type: object
          additionalProperties: false
          required: [model, modes]
          properties:
            model:
              type: string
            modes:
              type: object
              additionalProperties:
                type: object
                additionalProperties: false
                properties:
                  max_reasoning_tokens: { type: ["integer", "null"], minimum: 0 }
                  reasoning_effort: { type: ["string", "null"] }
                  user_prompt_prefix: { type: ["string", "null"] }
                  user_prompt_suffix: { type: ["string", "null"] }
                  temperature: { type: ["number", "null"], minimum: 0.0, maximum: 2.0 }
                  top_p: { type: ["number", "null"], minimum: 0.0, maximum: 1.0 }
  model_aliases:
    type: array
    items:
      type: object
      additionalProperties: false
      properties:
        pattern:
          type: string
        replacement:
          type: string
      required: [pattern, replacement]
    default: []
  sandboxing:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      strict_mode: { type: boolean }
      allow_parent_access: { type: boolean }
      custom_tool_patterns:
        type: array
        items: { type: string }
      excluded_tools:
        type: array
        items: { type: string }
      default_tool_patterns:
        type: array
        items: { type: string }
      path_parameter_names:
        type: array
        items: { type: string }
  responses_api:
    type: object
    additionalProperties: false
    properties:
      websocket:
        type: object
        additionalProperties: false
        properties:
          frontend_enabled: { type: boolean }
          backend_enabled: { type: boolean }
          frontend_path: { type: string }
          connection_timeout: { type: integer, minimum: 60, maximum: 7200 }
          max_connections: { type: integer, minimum: 1 }
  codebuff:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      websocket_path: { type: string }
      heartbeat_timeout_seconds: { type: integer, minimum: 1 }
      session_cleanup_hours: { type: integer, minimum: 1 }
      max_connections: { type: integer, minimum: 1 }
      max_message_size_bytes: { type: integer, minimum: 1024 }
  replacement:
    type: object
    description: >
      Deprecated random model replacement feature. Prefer composite weighted routing (`^` with optional `[weight=N]`)
      for new setups. When enabled, the proxy uses a compatibility bridge during the deprecation window and emits
      operator-facing deprecation metadata with an `N+1` removal timeline (removed in the release after deprecation).
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      probability: { type: number, minimum: 0.0, maximum: 1.0 }
      replacement_rules:
        type: array
        items:
          type: object
          required: [from_pattern, to_backend, to_model]
          properties:
            from_pattern: { type: string }
            to_backend: { type: string }
            to_model: { type: string }
      backend_model: { type: string }
      turn_count: { type: integer, minimum: 1 }
      allow_oauth_auto_replacement: { type: boolean }
  model_limit_enforcement:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
  reasoning_model_token_floor:
    type: object
    additionalProperties: false
    properties:
      enabled:
        type: boolean
        description: "Whether to enforce minimum output tokens for reasoning-first models"
      models:
        type: object
        additionalProperties: { type: integer, minimum: 1 }
        description: "Model ID (normalized) -> minimum output tokens"
  model_registry:
    type: object
    additionalProperties: false
    properties:
      download_enabled: { type: boolean }
      url: { type: string }
      update_interval_seconds: { type: integer, minimum: 0 }
      cache_path: { type: string }
      bootstrap_path: { type: string }
  vtc_client_patterns:
    type: array
    items: { type: string }
    default: ["cline", "kilo", "roo"]
  health_check:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      circuit_breaker_enabled: { type: boolean }
      notify_backends: { type: boolean }
      log_healthy_checks: { type: boolean }
      ping:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          interval_seconds: { type: number, minimum: 0.1 }
          timeout_seconds: { type: number, minimum: 0.1 }
          failure_threshold: { type: integer, minimum: 1 }
          count: { type: integer, minimum: 1 }
      http:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          interval_seconds: { type: number, minimum: 0.1 }
          timeout_seconds: { type: number, minimum: 0.1 }
          failure_threshold: { type: integer, minimum: 1 }
          method: { type: string, enum: [GET, HEAD] }
          path: { type: string }
          accept_any_response: { type: boolean }
  usage_window_warmup:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      entries:
        type: array
        items:
          type: object
          additionalProperties: false
          required: [model, time]
          properties:
            model: { type: string }
            time:
              type: string
              pattern: "^(?:[01]\\d|2[0-3]):[0-5]\\d$"
            execute_on_weekend: { type: boolean }
  failure_handling:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      max_silent_wait: { type: number, minimum: 0 }
      total_timeout_budget: { type: number, minimum: 0 }
      keepalive_interval: { type: number, minimum: 1 }
      max_failover_hops: { type: integer, minimum: 1 }
      min_retry_wait: { type: number, minimum: 0.1 }
  resilience:
    type: object
    additionalProperties: false
    properties:
      personal_backend_types:
        type: array
        items: { type: string }
      shared_backend_types:
        type: array
        items: { type: string }
      circuit_breaker:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          failure_threshold: { type: integer, minimum: 1, maximum: 100 }
          open_cooldown_seconds:
            type: number
            exclusiveMinimum: 0
            maximum: 3600
          half_open_success_threshold: { type: integer, minimum: 1, maximum: 10 }
          half_open_max_inflight: { type: integer, minimum: 1, maximum: 10 }
  routing:
    type: object
    additionalProperties: false
    properties:
      disable_backend_ids: { type: boolean }
      disable_backend_names: { type: boolean }
      disable_model_names: { type: boolean }
      model_only_preference_policy:
        type: string
        enum: [round_robin, cost, priority]
      model_only_model_overrides:
        type: object
        additionalProperties:
          type: string
          enum: [round_robin, cost, priority]
      model_only_backend_family_overrides:
        type: object
        additionalProperties:
          type: string
          enum: [round_robin, cost, priority]
      model_only_missing_cost:
        type: number
        minimum: 0
      model_only_missing_priority: { type: integer }
      capability_refresh_interval_seconds:
        type: number
        minimum: 0
      capability_refresh_backoff_seconds:
        type: number
        minimum: 0
  canonical_request_processing:
    type: object
    additionalProperties: false
    properties:
      empty_stream_recovery_prompt:
        type: string
        description: "Recovery prompt appended to retry requests when stream produces no content"
      max_empty_stream_retries:
        type: integer
        minimum: 0
        maximum: 5
        description: "Maximum number of empty stream retry attempts before failing"
  usage_tracking:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      persistence_path: { type: string }
      flush_interval_seconds: { type: number, minimum: 0.1 }
      max_records_in_memory: { type: integer, minimum: 1000 }
  end_of_session:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      emit_events: { type: boolean }
      detect_stream_signals: { type: boolean }
      detect_tool_completion: { type: boolean }
      emission_ttl_seconds: { type: integer, minimum: 0 }
      dispatch_timeout_seconds: { type: number, minimum: 0.0 }
  memory:
    type: object
    additionalProperties: false
    properties:
      available: { type: boolean }
      default_enabled: { type: boolean }
      summary_model: { type: ["string", "null"] }
      context_model: { type: ["string", "null"] }
      summary_prompt: { type: ["string", "null"] }
      context_prompt: { type: ["string", "null"] }
      database_path: { type: string }
      session_timeout_minutes: { type: integer, minimum: 1 }
      summarization_delay_seconds: { type: integer, minimum: 0 }
      max_sessions_to_consider: { type: integer, minimum: 1 }
      max_context_tokens: { type: integer, minimum: 1 }
      max_summary_tokens: { type: integer, minimum: 1 }
      max_transcript_chars: { type: integer, minimum: 1 }
      summary_completion_tokens: { type: integer, minimum: 1 }
      context_relevance_threshold: { type: number, minimum: 0, maximum: 1 }
      retention_days: { type: integer, minimum: 1 }
      max_buffer_size_bytes: { type: integer, minimum: 1024 }
      analysis_queue_maxsize: { type: integer, minimum: 1 }
      analysis_timeout_seconds: { type: integer, minimum: 1 }
      max_concurrent_analyses: { type: integer, minimum: 1 }
      context_template: { type: ["string", "null"] }
      redaction_patterns:
        type: array
        items: { type: string }
      persist_transcript: { type: boolean }
      disabled_users:
        type: array
        items: { type: string }
      disabled_clients:
        type: array
        items: { type: string }
      single_user_mode: { type: boolean }
      fixed_user_id: { type: ["string", "null"] }
      summary_prompt_version: { type: string }
      summary_schema_version: { type: string }
      require_project_discovery: { type: boolean }
      project_discovery_mode: { type: string, enum: [deterministic, nondeterministic, any] }
  database:
    type: object
    additionalProperties: false
    properties:
      url: { type: string }
      auto_migrate: { type: boolean }
      echo: { type: boolean }
      echo_pool: { type: boolean }
      pool_size: { type: integer, minimum: 1 }
      max_overflow: { type: integer, minimum: 0 }
      pool_timeout: { type: integer, minimum: 1 }
  compaction:
    type: object
    additionalProperties: false
    properties:
      enabled: { type: boolean }
      token_threshold: { type: integer, minimum: 0 }
      max_tokens: { type: integer, minimum: 0 }
      allowed_tool_categories:
        type: array
        items: { type: string }
      denied_tool_categories:
        type: array
        items: { type: string }
      max_stubs_per_resource: { type: integer, minimum: 0 }
      preserve_last_n_results: { type: integer, minimum: 0 }
      stub_template: { type: string }
      redact_resource_identifiers:
        type: boolean
        default: false
        description: "Redact file paths and commands in compaction stubs (trade-off: security vs debuggability)"
  dynamic_compression:
    type: object
    additionalProperties: true
    properties:
      enabled: { type: boolean }
      level: { type: string, enum: [conservative, balanced, aggressive] }
      max_level: { type: string, enum: [conservative, balanced, aggressive] }
      min_bytes: { type: integer, minimum: 0 }
      per_output_evaluation_log_level: { type: string, enum: [off, debug, info] }
      time_budget_ms_per_output: { type: integer, minimum: 1 }
      explicit_format_flags:
        type: array
        items: { type: string }
      telemetry_include_content_hashes: { type: boolean }
      alerts:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          failure_threshold: { type: integer, minimum: 1 }
          fallback_threshold: { type: integer, minimum: 1 }
          window_seconds: { type: integer, minimum: 1 }
          cooldown_seconds: { type: integer, minimum: 1 }
      recovery:
        type: object
        additionalProperties: false
        properties:
          mode: { type: string, enum: [never, failures, always] }
          min_original_bytes: { type: integer, minimum: 0 }
          min_saved_bytes: { type: integer, minimum: 0 }
          max_artifact_bytes: { type: integer, minimum: 128 }
          max_artifacts: { type: integer, minimum: 1 }
          retention_seconds: { type: integer, minimum: 1 }
          storage_dir: { type: string, minLength: 1 }
          hint_in_text: { type: boolean }
      disable_categories:
        type: array
        items: { type: string }
      disable_methods:
        type: array
        items: { type: string }
      disable_tools:
        type: array
        items: { type: string }
      disable_command_prefixes:
        type: array
        items: { type: string }
      disable_tool_name_substrings:
        type: array
        items: { type: string }
      categories:
        type: object
        additionalProperties: { type: boolean }
      methods:
        type: object
        additionalProperties: { type: boolean }
      noise_directories:
        type: array
        items: { type: string }
      search_context_lines: { type: integer, minimum: 0 }
      search_max_matches_per_file: { type: integer, minimum: 1 }
      search_max_total_groups: { type: integer, minimum: 1 }
      search_max_line_length: { type: integer, minimum: 20 }
      file_detail_mode:
        type: string
        enum: [auto, full, structure, signatures]
      file_detail_fallback_mode:
        type: string
        enum: [full, structure, signatures]
      file_detail_auto_full_max_lines: { type: integer, minimum: 1 }
      file_detail_auto_structure_max_lines: { type: integer, minimum: 1 }
      file_detail_include_line_numbers: { type: boolean }
      file_detail_max_lines: { type: ["integer", "null"], minimum: 0 }
      file_detail_last_n_lines: { type: ["integer", "null"], minimum: 0 }
      output_pattern_rules:
        type: array
        items:
          type: object
          additionalProperties: false
          required: [pattern, message]
          properties:
            pattern: { type: string, minLength: 1 }
            message: { type: string }
            unless: { type: ["string", "null"] }
            fallback_message: { type: string, minLength: 1 }
      output_pattern_regex_timeout_ms: { type: integer, minimum: 1 }
      declarative_rules:
        type: array
        items:
          type: object
      declarative_rule_files:
        type: array
        items: { type: string, minLength: 1 }
      declarative_regex_timeout_ms: { type: integer, minimum: 1 }
      diff_max_lines_per_hunk: { type: integer, minimum: 1 }
      diff_max_total_lines: { type: integer, minimum: 10 }
      json_structural_max_depth: { type: integer, minimum: 1 }
      json_structural_max_keys_per_object: { type: integer, minimum: 1 }
      json_structural_max_array_elements: { type: integer, minimum: 1 }
      json_structural_string_max_len: { type: integer, minimum: 8 }
      json_structural_min_bytes: { type: integer, minimum: 0 }
      xml_safeguard_text_max_len: { type: integer, minimum: 16 }
      xml_safeguard_min_bytes: { type: integer, minimum: 0 }
      log_dedupe_min_repeat: { type: integer, minimum: 2 }
      log_dedupe_min_bytes: { type: integer, minimum: 0 }
      sensitive_projection_skip_prefixes:
        type: array
        items: { type: string }
      marker:
        type: object
        additionalProperties: false
        properties:
          enabled: { type: boolean }
          style: { type: string, enum: [prefix, suffix, none] }
          include_sizes: { type: boolean }
          include_methods: { type: boolean }
          structured_payload_mode: { type: string, enum: [out_of_band_only] }
      rules:
        type: array
        items:
          type: object
          additionalProperties: false
          properties:
            name: { type: string }
            priority: { type: integer, minimum: 0 }
            when:
              type: object
              additionalProperties: false
              properties:
                tool_name: { type: ["string", "null"] }
                tool_category: { type: ["string", "null"] }
                command_signature: { type: ["string", "null"] }
                command_prefix: { type: ["string", "null"] }
                has_explicit_format: { type: ["boolean", "null"] }
                min_bytes: { type: ["integer", "null"], minimum: 0 }
                max_bytes: { type: ["integer", "null"], minimum: 0 }
                content_types:
                  type: array
                  items: { type: string }
            pipeline:
              type: array
              items: { type: string }
  non_forwardable_tagging:
    type: object
    additionalProperties: false
    properties:
      max_identities_per_session:
        type: integer
        minimum: 1
        default: 10000
        description: "Maximum number of stored non-forwardable identities per session"
