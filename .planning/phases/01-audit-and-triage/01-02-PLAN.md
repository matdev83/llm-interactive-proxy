---
phase: 01-audit-and-triage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-audit-and-triage/RISK-AUDIT.md
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "Loop-detection behavior is documented as active, partial, or defunct with code evidence"
    - "MCP client placeholder scope is documented with a clear boundary between real and synthetic behavior"
    - "Dead configuration variants in src/core/domain/configuration/ are identified and confirmed safe to remove"
    - "The MagicMock production fallback risk in src/core/app/controllers/__init__.py is assessed and documented"
    - "Dependency pinning gaps in pyproject.toml are listed with risk assessment"
  artifacts:
    - path: ".planning/phases/01-audit-and-triage/RISK-AUDIT.md"
      provides: "Risk and debt audit covering all five OPS-02 audit areas"
      contains: "loop-detection, MCP, MagicMock, dead config, dependency pinning"
  key_links:
    - from: ".planning/phases/01-audit-and-triage/RISK-AUDIT.md"
      to: "src/core/services/loop_breaking_service.py"
      via: "loop detection status assessment"
      pattern: "loop_breaking|vtc_detection|tool_call_loop"
    - from: ".planning/phases/01-audit-and-triage/RISK-AUDIT.md"
      to: "src/core/app/controllers/__init__.py"
      via: "MagicMock risk assessment"
      pattern: "MagicMock|mock_processor"
---

<objective>
Audit five specific risk areas identified in the brownfield planning: loop detection, MCP placeholder scope, MagicMock production fallback, dead configuration variants, and dependency pinning gaps.

Purpose: OPS-02 requires project documentation to stay synchronized with brownfield architecture and operational constraints. This audit produces honest, evidence-backed assessments of each risk area without changing any behavior.
Output: RISK-AUDIT.md with one section per risk area, each with code evidence and a risk rating.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: audit loop detection, MCP scope, and MagicMock risk</name>
  <files>
    src/core/services/loop_breaking_service.py
    src/core/services/tool_call_loop_middleware.py
    src/core/services/vtc_detection.py
    src/core/services/edit_precision_patterns.py
    src/core/services/universal_mcp_client.py
    src/core/app/controllers/__init__.py
  </files>
  <action>
    Read each file listed above. For each of the three audit areas, collect evidence:

    **Loop Detection (loop_breaking_service.py, tool_call_loop_middleware.py, vtc_detection.py, edit_precision_patterns.py):**
    - Is the loop detection logic actively called in the request path? Trace call sites.
    - Does it have test coverage? (grep tests/ for loop_breaking, vtc_detection, tool_call_loop)
    - Are there TODO/FIXME/disabled markers?
    - Classify as: active (wired + tested), partial (wired but untested or incomplete), or defunct (not wired or explicitly disabled)

    **MCP Client Placeholder Scope (universal_mcp_client.py):**
    - List every method that contains TODO, placeholder, synthetic, or pass-only bodies
    - List every method that has real protocol-backed implementation
    - Note what a caller would observe when hitting a placeholder vs a real method
    - Classify boundary: what is real vs synthetic

    **MagicMock Production Fallback (controllers/__init__.py):**
    - Find the MagicMock fallback path (already known around line 174-192)
    - Determine: under what runtime conditions is this path reachable in production?
    - Is there a guard that prevents it outside test environments?
    - Assess risk: can a misconfigured production deployment silently serve mock responses?

    Do NOT change any code. Read only.
  </action>
  <verify>You have concrete evidence notes for all three areas before proceeding to task 2.</verify>
  <done>Loop detection status, MCP placeholder boundary, and MagicMock reachability all assessed with file+line evidence.</done>
</task>

<task type="auto">
  <name>task 2: audit dead config variants and dependency pinning, then write RISK-AUDIT.md</name>
  <files>
    src/core/domain/configuration/reasoning_config.py
    src/core/domain/configuration/reasoning_config_new.py
    src/core/domain/configuration/reasoning_config_new2.py
    pyproject.toml
    .planning/phases/01-audit-and-triage/RISK-AUDIT.md
  </files>
  <action>
    **Dead Configuration Variants:**
    - Read all three reasoning_config*.py files
    - For each: note what class/model it defines and whether it is imported anywhere in src/ (grep src/ for the class name)
    - Identify which variants are unused (zero import sites outside their own file)
    - Confirm whether removing the unused variants would be safe (no call sites)

    **Dependency Pinning Gaps (pyproject.toml):**
    - Read the [project] dependencies list
    - For each dependency, classify pinning:
      - pinned: exact version (e.g. `openai==1.84.0`)
      - range-pinned: lower bound only (e.g. `pydantic>=2`, `cbor2>=5.6.0`)
      - unpinned: no version constraint (e.g. `fastapi`, `uvicorn[standard]`, `anthropic`)
    - For unpinned packages, assess risk: is this a package known for breaking changes between minor versions?

    Then write .planning/phases/01-audit-and-triage/RISK-AUDIT.md with this structure:

    ```markdown
    # Risk Audit

    **Phase:** 01-audit-and-triage
    **Date:** {today}
    **Auditor:** OpenCode

    ## 1. Loop Detection

    **Status:** active | partial | defunct

    ### Evidence
    - {file}:{line} — {observation}

    ### Assessment
    {1-3 sentences on what this means operationally}

    ---

    ## 2. MCP Client Placeholder Scope

    **Real behavior:** {list of methods with real implementations}
    **Synthetic behavior:** {list of methods that return placeholder/TODO responses}

    ### Evidence
    - {file}:{line} — {observation}

    ### Assessment
    {1-3 sentences on what a caller observes and what the operational risk is}

    ---

    ## 3. MagicMock Production Fallback

    **Reachable in production:** yes | no | conditional

    ### Evidence
    - {file}:{line} — {observation}

    ### Assessment
    {1-3 sentences on the risk and what conditions trigger it}

    ---

    ## 4. Dead Configuration Variants

    | File | Class | Import Sites | Safe to Remove |
    |------|-------|-------------|----------------|
    | reasoning_config_new2.py | ReasoningConfigurationNew2 | 0 | yes |
    ...

    ### Assessment
    {1-2 sentences}

    ---

    ## 5. Dependency Pinning Gaps

    | Package | Current Spec | Pinning | Risk |
    |---------|-------------|---------|------|
    | fastapi | (none) | unpinned | high — frequent breaking changes in minor versions |
    ...

    ### Assessment
    {1-2 sentences on overall pinning posture}

    ---

    ## Pre-existing LSP Errors

    The following LSP errors were present before this audit and are not caused by planning work:

    | File | Error |
    |------|-------|
    | src/connectors/nvidia.py | Unnecessary isinstance call (line 145) |
    | src/core/config/env/util.py | Unnecessary isinstance / always-true conditions (lines 35, 80, 100) |
    | src/core/services/backend_plugin_discovery.py | Unnecessary isinstance / always-false condition (lines 191, 222) |
    | src/core/app/application_builder.py | Always-true condition (line 664) |
    ```
  </action>
  <verify>File .planning/phases/01-audit-and-triage/RISK-AUDIT.md exists and contains all five numbered sections with evidence and assessment text.</verify>
  <done>All five OPS-02 audit areas documented with code evidence, risk ratings, and assessments. Pre-existing LSP errors catalogued.</done>
</task>

</tasks>

<verification>
- RISK-AUDIT.md exists with sections 1-5
- Loop detection has a status of active/partial/defunct with file:line evidence
- MCP section lists real vs synthetic methods explicitly
- MagicMock section states whether production reachability is yes/no/conditional
- Dead config table lists all three reasoning_config variants with import site counts
- Dependency pinning table covers all unpinned packages with risk assessment
</verification>

<success_criteria>
OPS-02 partially satisfied: five risk areas documented with honest, evidence-backed assessments. No behavior changed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-and-triage/01-02-SUMMARY.md`
</output>
