---
phase: 02-compatibility-contract-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/connectors/test_openai_compat_contracts.py
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "OpenAI streaming responses through the proxy produce spec-shaped SSE chunks (id, object, choices[].delta)"
    - "OpenAI tool-call responses carry spec-shaped tool_calls[].function fields in choices[].delta"
    - "OpenAI error responses carry spec-shaped {error: {message, type, code}} JSON bodies"
  artifacts:
    - path: "tests/unit/connectors/test_openai_compat_contracts.py"
      provides: "Contract tests for OpenAI streaming, tool-call, and error-shape behaviors"
      exports: []
  key_links:
    - from: "tests/unit/connectors/test_openai_compat_contracts.py"
      to: "src/connectors/openai.py"
      via: "OpenAIConnector.chat_completions() with streaming=True and tool-call fixtures"
      pattern: "StreamingResponseEnvelope|ResponseEnvelope"
    - from: "tests/unit/connectors/test_openai_compat_contracts.py"
      to: "src/core/transport/fastapi/exception_adapters.py"
      via: "error shape assertions on _build_openai_error_envelope or equivalent"
      pattern: "error.*message.*type.*code"
---

<objective>
Add contract tests that assert OpenAI-compatible behavioral parity for streaming, tool-calls, and error responses through the proxy connector layer.

Purpose: COMP-01 requires the OpenAI-compatible endpoint to maintain behavioral parity with the OpenAI spec. The existing canonical tests verify the API contract shape but do not assert spec-shaped output for streaming chunks, tool-call deltas, or error envelopes. This plan closes that gap with focused contract tests.

Output: `tests/unit/connectors/test_openai_compat_contracts.py` with streaming, tool-call, and error-shape contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/connectors/openai.py
@src/connectors/contracts/__init__.py
@src/core/domain/responses.py
@src/core/transport/fastapi/exception_adapters.py
@tests/unit/connectors/test_openai_canonical.py
</context>

<interfaces>
<!-- Key types the executor needs. No codebase exploration required. -->

From src/connectors/contracts/__init__.py:
```python
@dataclass
class ConnectorChatCompletionsRequest(InternalDTO):
    request: CanonicalChatRequest
    processed_messages: Sequence[ChatMessage]
    effective_model: str
    identity: IAppIdentityConfig | None
    cancellation_token: SessionKey | None
    cancellation_coordinator: ISessionCancellationCoordinator | None
    context: ConnectorRequestContext | None
    options: dict[str, JsonValue] = field(default_factory=dict)
```

From src/core/domain/chat.py (inferred from canonical tests):
```python
class CanonicalChatRequest:
    model: str
    messages: list[ChatMessage]
    max_tokens: int | None
    stream: bool = False
    tools: list[...] | None = None
```

From src/core/domain/responses.py (inferred from canonical tests):
```python
class ResponseEnvelope: ...          # non-streaming
class StreamingResponseEnvelope: ... # streaming — has async iterator of chunks
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: OpenAI streaming chunk shape contract tests</name>
  <files>tests/unit/connectors/test_openai_compat_contracts.py</files>
  <behavior>
    - Test 1: Streaming response from OpenAIConnector yields chunks where each chunk has `id`, `object="chat.completion.chunk"`, `choices` list
    - Test 2: Each streaming chunk's choices[0].delta has a `role` or `content` field (not empty dict)
    - Test 3: Final streaming chunk has choices[0].finish_reason set (not None)
    - Test 4: Non-streaming response has `id`, `object="chat.completion"`, `choices[0].message.content`, `usage` fields
  </behavior>
  <action>
    Create `tests/unit/connectors/test_openai_compat_contracts.py`.

    Use the same fixture pattern as `tests/unit/connectors/test_openai_canonical.py` — mock `httpx.AsyncClient`, create `OpenAIConnector`, set `api_key` and `api_base_url`, call `disable_health_check()`.

    For streaming tests: mock the HTTP client to return a realistic SSE byte stream (e.g. `data: {"id":"chatcmpl-1","object":"chat.completion.chunk","choices":[{"delta":{"role":"assistant"},"finish_reason":null}]}\n\ndata: {"id":"chatcmpl-1","object":"chat.completion.chunk","choices":[{"delta":{"content":"Hello"},"finish_reason":null}]}\n\ndata: {"id":"chatcmpl-1","object":"chat.completion.chunk","choices":[{"delta":{},"finish_reason":"stop"}]}\n\ndata: [DONE]\n\n`).

    Assert that `StreamingResponseEnvelope` is returned and that iterating its chunks produces objects with the required fields. Read `src/core/domain/responses.py` first to understand the exact chunk shape returned by the connector.

    For non-streaming tests: mock HTTP client to return a standard OpenAI completion JSON response. Assert `ResponseEnvelope` fields.

    Keep tests focused on shape assertions — do not test business logic.
  </action>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/unit/connectors/test_openai_compat_contracts.py::TestOpenAIStreamingContractShape -x -q</automated>
  </verify>
  <done>4 streaming/non-streaming shape tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: OpenAI tool-call and error-shape contract tests</name>
  <files>tests/unit/connectors/test_openai_compat_contracts.py</files>
  <behavior>
    - Test 5: Tool-call streaming chunk has choices[0].delta.tool_calls[0] with id, type="function", function.name, function.arguments fields
    - Test 6: Tool-call non-streaming response has choices[0].message.tool_calls[0] with id, type="function", function.name, function.arguments
    - Test 7: When OpenAIConnector raises a backend error, the error envelope has shape {error: {message: str, type: str, code: str|int|None}} — assert this by calling the exception adapter directly with a mock request
    - Test 8: 401 backend error maps to HTTP 401 with error.type="authentication_error"
    - Test 9: 429 backend error maps to HTTP 429 with error.type="rate_limit_error" or error.code containing "rate_limit"
  </behavior>
  <action>
    Add tool-call and error-shape tests to `tests/unit/connectors/test_openai_compat_contracts.py`.

    For tool-call tests: mock HTTP client to return a streaming SSE response containing a tool-call delta chunk:
    `data: {"id":"chatcmpl-1","object":"chat.completion.chunk","choices":[{"delta":{"tool_calls":[{"index":0,"id":"call_abc","type":"function","function":{"name":"get_weather","arguments":""}}]},"finish_reason":null}]}\n\n`
    followed by argument chunks and a finish chunk. Assert the tool_calls structure on the yielded chunk.

    For error-shape tests: import the error-building function from `src/core/transport/fastapi/exception_adapters.py`. Read that file first to find the correct function name (look for functions that build OpenAI-shaped error dicts). Call it with mock inputs and assert the `{error: {message, type, code}}` envelope shape.

    Do NOT mock the exception adapter internals — call it as a unit and assert its output shape.
  </action>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/unit/connectors/test_openai_compat_contracts.py -x -q</automated>
  </verify>
  <done>All 9 contract tests pass. Tool-call delta shape and error envelope shape are explicitly asserted.</done>
</task>

</tasks>

<verification>
./.venv/Scripts/python.exe -m pytest tests/unit/connectors/test_openai_compat_contracts.py -v
./.venv/Scripts/python.exe -m pytest tests/unit/connectors/test_openai_canonical.py -q
</verification>

<success_criteria>
- 9 contract tests pass covering streaming chunk shape, tool-call delta shape, and error envelope shape
- No existing OpenAI connector tests broken
- Tests are self-contained (no live network calls)
</success_criteria>

<output>
After completion, create `.planning/phases/02-compatibility-contract-stabilization/02-02-SUMMARY.md`
</output>
